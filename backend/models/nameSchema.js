const mongoose = require("mongoose");

let nameSchema = new mongoose.Schema(
  {
    name: {
      type: String,
      required: true,
    },
  },
  { timestamps: true }
);

module.exports = mongoose.model("Usercollection", nameSchema);
